// 事件处理
import Vue from "..";

/**
 * 初始化事件
 * @param vm vue对象
 */
export function initEvent(vm: Vue) {
  initCanvasEvent(vm)
}
/**
 * 初始化canvas事件
 * @param vm vue对象
 */
function initCanvasEvent(vm: Vue) {
  
}